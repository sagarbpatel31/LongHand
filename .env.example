# Copy to .env and fill in. .env is gitignored — never commit a real key.
#
# The raw AssemblyAI key. It is sent as the Authorization header value
# VERBATIM — there is NO "Bearer " prefix. An invalid key returns 404, not 401.
ASSEMBLYAI_API_KEY=your_raw_key_here
